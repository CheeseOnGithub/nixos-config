[package]
name = "myproject"
version = "0.1.0"
edition = "2024"

[profile.release]
strip = false
lto = false
codegen-units = 16
panic = "unwind"

[dependencies]
windows = { version = "0.62.2", features = [
  "Win32_UI_WindowsAndMessaging",
] }
